<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:stretchColumns="*"
    tools:context=".activity.InventoryActivity">

    <TableRow android:layout_width="fill_parent">

        <TextView
            android:id="@+id/inventory_page_heading"
            android:text="@string/inventory_page_heading_str"
            android:textSize="@dimen/activity_header_size" />

    </TableRow>

    <TableRow>

        <Button
            android:id="@+id/add_inventory_item_btn"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:onClick="displayAddInventory"
            android:paddingBottom="@dimen/main_btn_vertical_margin"
            android:paddingTop="@dimen/main_btn_vertical_margin"
            android:text="@string/add_inventory_item_btn_str" />

    </TableRow>

    <TableRow>

        <TableLayout
            android:id="@+id/item_table"
            android:layout_width="fill_parent"
            android:stretchColumns="*" />
    </TableRow>

    <TableRow>

        <TableLayout android:id="@+id/item_detail_table">

            <TableRow>

                <TextView android:id="@+id/item_detail_name" />

            </TableRow>

            <TableRow>

                <TextView android:id="@+id/item_detail_product_code" />
            </TableRow>

            <TableRow>

                <TextView android:id="@+id/item_detail_price" />
            </TableRow>
            <TableRow>
                <TextView android:id="@+id/item_test_view"/>
            </TableRow>
        </TableLayout>
    </TableRow>
</TableLayout>
