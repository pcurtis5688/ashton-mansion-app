<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/standardBackground"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:stretchColumns="*"
    tools:context="com.ashtonmansion.ashtonmansioncloverapp.activity.MainActivity">

    <TableRow>

        <ImageView
            android:layout_span="2"
            android:src="@drawable/ashtonmansionlogo" />
    </TableRow>

    <TableRow>

        <Button
            android:id="@+id/show_appt_page_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/text_margin"
            android:background="@color/standardButtonBackground"
            android:onClick="showApptPage"
            android:paddingBottom="@dimen/nominal_padding"
            android:paddingTop="@dimen/nominal_padding"
            android:text="@string/appt_page"
            android:textColor="@color/standardButtonTextColor" />

        <Button
            android:id="@+id/show_inventory_page_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/text_margin"
            android:background="@color/standardButtonBackground"
            android:onClick="showInventoryPage"
            android:paddingBottom="@dimen/nominal_padding"
            android:paddingTop="@dimen/nominal_padding"
            android:text="@string/inventory_page_btn_str"
            android:textColor="@color/standardButtonTextColor" />
    </TableRow>

    <TableRow>

        <Button
            android:id="@+id/show_emp_page_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/text_margin"
            android:background="@color/standardButtonBackground"
            android:onClick="showEmployeePage"
            android:paddingBottom="@dimen/nominal_padding"
            android:paddingTop="@dimen/nominal_padding"
            android:text="@string/employee_page_btn_str"
            android:textColor="@color/standardButtonTextColor" />

        <Button
            android:id="@+id/show_cust_page_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/text_margin"
            android:background="@color/standardButtonBackground"
            android:onClick="showCustPage"
            android:paddingBottom="@dimen/nominal_padding"
            android:paddingTop="@dimen/nominal_padding"
            android:text="@string/label_cust_page_btn"
            android:textColor="@color/standardButtonTextColor" />
    </TableRow>

    <TableRow>

        <Button
            android:id="@+id/settings_btn"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/text_margin"
            android:layout_span="2"
            android:background="@color/standardButtonBackground"
            android:onClick="showSettingsPage"
            android:paddingBottom="@dimen/nominal_padding"
            android:paddingTop="@dimen/nominal_padding"
            android:text="@string/settings_btn_str"
            android:textColor="@color/standardButtonTextColor" />
    </TableRow>

    <TableRow>

        <Button
            android:id="@+id/management_btn"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/text_margin"
            android:layout_span="2"
            android:background="@color/standardButtonBackground"
            android:onClick="showManagementPage"
            android:paddingBottom="@dimen/nominal_padding"
            android:paddingTop="@dimen/nominal_padding"
            android:text="@string/management_page_btn_str"
            android:textColor="@color/standardButtonTextColor" />
    </TableRow>
</TableLayout>